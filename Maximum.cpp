#include<iostream>
using namespace std;
int main(int argc, char const *argv[])
{
    int arr[5];
   cout<<"the size of array is :"<<sizeof(arr)<<endl;
   // int arr[6];                             // array once declared cannot be changed 
   // cout<<"the size of array is :"<<sizeof(arr)<<endl; // hence we use vectors ....
    return 0;
}
